# Changelog - 2026-04-23

## Summary
Cleanup of empty integration test stubs, archival of completed-feature docs (type conversion), and removal of a phantom `guests` resource from the UI test runner.

## Changes

- [tests/integration/test-categories.js] Removed (empty stub).
- [tests/integration/test-product-create.js] Removed (empty stub).
- [tests/integration/test-xml-format.js] Removed (empty stub).
- [tests/integration/test-xml.js] Removed (empty stub).
- [doc/CHANGELOG_TYPE_CONVERSION.md] Moved to `doc/archive/` (residue of completed type-conversion feature).
- [doc/IMPLEMENTATION_SUMMARY.md] Moved to `doc/archive/` (residue of completed type-conversion feature).
- [doc/QUICK_START_TYPE_CONVERSION.md] Moved to `doc/archive/` (residue of completed type-conversion feature).
- [tests/ui/run-integration-tests.py] Removed the `guests` resource entry; it was hardcoded in the test runner but never declared in `PRESTASHOP_RESOURCES` (types.ts), causing a persistent false-positive failure.

## Notes
Three docs relocated rather than deleted to follow the existing project convention (`doc/archive/` already contains obsolete docs like `FINAL_REPORT.md`, `NEXT_STEPS.md`, etc.). No references to these files exist outside themselves (README and root docs are unaffected).
